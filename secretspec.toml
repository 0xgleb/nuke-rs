[project]
name = "nuke"
revision = "1.0"

[profiles.default]
ETH_WS_RPC_URL = { description = "Ethereum mainnet JSON-RPC websocket URL (with provider key embedded)", required = false }

[profiles.production]
ETH_WS_RPC_URL = { required = true }

[profiles.test]
# Empty: e2e test points the example at an in-process mock; no secrets needed.
